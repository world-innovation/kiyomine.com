## Cursor Cloud specific instructions

This repository (`kiyomine.com`) is currently an empty/freshly-initialized project. As of its initial state, it contains only a single placeholder file (`BoldHope`) and no source code, dependencies, build tooling, or services.

- **No package manager or dependencies** are configured. There is no `package.json`, `requirements.txt`, `Gemfile`, `go.mod`, or any other dependency manifest.
- **No lint, test, or build commands** exist. There are no configuration files for ESLint, Prettier, pytest, Jest, or similar tools.
- **No services** need to be started. There are no Docker, docker-compose, or devcontainer configurations.
- **No README or documentation** exists yet.

When code is added to this repository, this section should be updated with relevant setup, run, and test instructions.
